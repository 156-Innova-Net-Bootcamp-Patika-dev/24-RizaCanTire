export default class ResidenceAddRangeModel{
    constructor(blockNumber,floorPerBlock,housePerBlock,residenceTypeId ) {
        this.blockNumber = blockNumber;
        this.floorPerBlock = floorPerBlock;
        this.housePerBlock = housePerBlock;
        this.residenceTypeId = residenceTypeId; 
      }
}