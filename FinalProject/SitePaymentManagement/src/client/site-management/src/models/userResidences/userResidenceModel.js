export default class UserResidenceModel {
  constructor(userId, residenceId,residentTypeId) {
    this.userId = userId;
    this.residenceId = residenceId;
    this.residentTypeId = residentTypeId;

  }
}